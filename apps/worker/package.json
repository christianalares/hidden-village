{
  "name": "@hidden-village/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No worker tests yet'",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@bull-board/api": "^7.0.0",
    "@bull-board/hono": "^7.0.0",
    "@hidden-village/jobs": "workspace:*",
    "@hono/node-server": "^2.0.2",
    "bullmq": "^5.76.7",
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
