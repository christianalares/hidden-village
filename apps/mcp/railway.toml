[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @hidden-village/mcp build"
watchPatterns = [
  "/apps/mcp/**",
  "/packages/db/**",
  "/packages/finance/**",
  "/packages/storage/**",
  "/package.json",
  "/pnpm-lock.yaml",
  "/pnpm-workspace.yaml",
]

[deploy]
preDeployCommand = "pnpm --filter @hidden-village/db db:migrate:prod"
startCommand = "pnpm --filter @hidden-village/mcp start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
